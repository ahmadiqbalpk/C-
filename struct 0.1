#include<iostream>
using namespace std;

    struct Inventory {
        int PartNum;
        char description;
        double price;
        
    };
void setItem(Inventory &part)  // used & reference operator
{
    
    cout << "Enter name";
    cin >> part.PartNum;
  
    cout << "ener price";
    cin >> part.price;
}

void ShowPart(Inventory part)
{
    cout << part.PartNum << endl;
    cout << part.price << endl;
}

int main()
{
    Inventory part;
    
    setItem(part); // argument passed by reference
    ShowPart(part); // argument passed by value 
    return 0;
    
}
 
